<template>
    <div>
        <div>
            <h1 class="head_vision">Citizen Journalism Platform for Innovative Solution</h1>
        </div>
        <div class="how_to">
            <p>bergabunglah bersama kami untuk...</p>
            <h2>Share ur Innovation</h2>
            <input class="upload_button_2" type="button" value="UPLOAD">
        </div>
        <div class="misi">
            <h2>Why Sharing?</h2>
            <img class="g_t" align="left" src="../assets/S__17219904.jpg">
            <p>Vue.js uses an HTML-based template syntax that allows you to declaratively bind the rendered DOM to the
                underlying Vue instance’s data. All Vue.js templates are valid HTML that can be parsed by spec-compliant
                browsers and HTML parsers. Under the hood, Vue compiles the templates into Virtual DOM render functions.
                Combined with the reactivity system, Vue is able to intelligently figure out the minimal number of
                components to re-render and apply the minimal amount of DOM manipulations when the app state changes.
                Vue.js uses an HTML-based template syntax that allows you to declaratively bind the rendered DOM to the
                underlying Vue instance’s data. All Vue.js templates are valid HTML that can be parsed by spec-compliant
                browsers and HTML parsers. Under the hood, Vue compiles the templates into Virtual DOM render functions.
                Combined with the reactivity system, Vue is able to intelligently figure out the minimal number of
                components to re-render and apply the minimal amount of DOM manipulations when the app state changes.
                Vue.js uses an HTML-based template syntax that allows you to declaratively bind the rendered DOM to the
                underlying Vue instance’s data. All Vue.js templates are valid HTML that can be parsed by spec-compliant
                browsers and HTML parsers. Under the hood, Vue compiles the templates into Virtual DOM render functions.
                Combined with the reactivity system, Vue is able to intelligently figure out the minimal number of
                components to re-render and apply the minimal amount of DOM manipulations when the app state
                changes.</p>
        </div>
        <div class="top_konten">
            <h2>bafe.id</h2>
            <img class="g_t" align="left" src="../assets/S__17219904.jpg">
            <p>Vue.js uses an HTML-based template syntax that allows you to declaratively bind the rendered DOM to the
                underlying Vue instance’s data. All Vue.js templates are valid HTML that can be parsed by spec-compliant
                browsers and HTML parsers. Under the hood, Vue compiles the templates into Virtual DOM render functions.
                Combined with the reactivity system, Vue is able to intelligently figure out the minimal number of
                components to re-render and apply the minimal amount of DOM manipulations when the app state changes.
                Vue.js uses an HTML-based template syntax that allows you to declaratively bind the rendered DOM to the
                underlying Vue instance’s data. All Vue.js templates are valid HTML that can be parsed by spec-compliant
                browsers and HTML parsers. Under the hood, Vue compiles the templates into Virtual DOM render functions.
                Combined with the reactivity system, Vue is able to intelligently figure out the minimal number of
                components to re-render and apply the minimal amount of DOM manipulations when the app state changes.
                Vue.js uses an HTML-based template syntax that allows you to declaratively bind the rendered DOM to the
                underlying Vue instance’s data. All Vue.js templates are valid HTML that can be parsed by spec-compliant
                browsers and HTML parsers. Under the hood, Vue compiles the templates into Virtual DOM render functions.
                Combined with the reactivity system, Vue is able to intelligently figure out the minimal number of
                components to re-render and apply the minimal amount of DOM manipulations when the app state
                changes.</p>
        </div>
        <div class="4_artikel">
            <table>
                <tr>
                    <td class="artikel_t"><a class="artikel_te" href="konten.html">
                        <img class="g_t" align="left" src="../assets/S__17219904.jpg">
                        <h3>bafe.id</h3>
                        Vue.js uses an HTML-based template syntax that allows you to declaratively bind the rendered DOM
                        to the underlying Vue instance’s data. All Vue.js templates are valid HTML that can be parsed by
                        spec-compliant browsers and HTML parsers. Under the hood, Vue compiles the templates into
                        Virtual DOM render functions.</a></td>
                    <td class="artikel_t"><a class="artikel_te" href="konten.html">
                        <img class="g_t" align="left" src="../assets/S__17219904.jpg">
                        <h3>bafe.id</h3>
                        Vue.js uses an HTML-based template syntax that allows you to declaratively bind the rendered DOM
                        to the underlying Vue instance’s data. All Vue.js templates are valid HTML that can be parsed by
                        spec-compliant browsers and HTML parsers. Under the hood, Vue compiles the templates into
                        Virtual DOM render functions</a></td>
                    <td class="artikel_t"><a class="artikel_te" href="konten.html">
                        <img class="g_t" align="left" src="../assets/S__17219904.jpg">
                        <h3>bafe.id</h3>
                        Vue.js uses an HTML-based template syntax that allows you to declaratively bind the rendered DOM
                        to the underlying Vue instance’s data. All Vue.js templates are valid HTML that can be parsed by
                        spec-compliant browsers and HTML parsers. Under the hood, Vue compiles the templates into
                        Virtual DOM render functions</a></td>
                    <td class="artikel_t"><a class="artikel_te" href="konten.html">
                        <img class="g_t" align="left" src="../assets/S__17219904.jpg">
                        <h3>bafe.id</h3>
                        Vue.js uses an HTML-based template syntax that allows you to declaratively bind the rendered DOM
                        to the underlying Vue instance’s data. All Vue.js templates are valid HTML that can be parsed by
                        spec-compliant browsers and HTML parsers. Under the hood, Vue compiles the templates into
                        Virtual DOM render functions.</a></td>
                </tr>
            </table>
        </div>
    </div>
</template>

<script>
export default {
  name: 'home'
}
</script>

<style scoped>
    .head_vision {
        text-align: center;
        color: #385446;
    }
    .how_to {
        margin: 0 auto;
        max-height: 300px;
        padding: 20px 40px;
        text-align: center;
        border: 0px;
        color: #fafafa;
        background-color: #385446;
        background-image: url('../assets/orang_ramai.jpg');
        background-position: 50% 0px;
        background-repeat: no-repeat;
    }
    .upload_button_2 {
        position: relative;
        padding: 15px 20px;
        border: 5px solid #fafafa;
        border-radius: 8px;
        background-color: #385446;
        color: #fafafa;
    }
    .misi {
        margin: 0 auto;
        width: 90%;
        max-height: 300px;
        padding: 20px 40px;
        text-align: justify;
        border: 0px;
        color: #000000;
    }
    .g_t {
        max-height: 150px;
        max-width: 300px;
        padding-right: 5px;
    }
    .top_konten {
        margin: 0 auto;
        width: 75%;
        max-height: 300px;
        padding: 20px 40px;
        text-align: justify;
        border: 0px;
        background-color: grey;
    }
    table {
        width: 100%;
        height: 200px;
        max-height: 400px;
        padding: 0px 8px;
        border-spacing: 10px;
    }
    .artikel_t {
        background-color: #385446;
        padding: 5px;
    }

    a {
        color: #fafafa;
        text-decoration: none;
    }
</style>
